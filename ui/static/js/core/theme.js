// theme.js — theme is locked to dark-red; light mode removed.
// Kept as a stub so any existing imports don't break at runtime.

export function initTheme() {
  // No-op: theme is fixed to dark in base.html's inline script.
}

export function toggleTheme() {
  // No-op: light mode removed.
}
